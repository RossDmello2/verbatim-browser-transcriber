# Verbatim does not read server-side environment variables at runtime.
#
# The maintained package lives in:
#   production ready/
#
# Provider API keys are entered in the browser UI and must not be committed.

VERBATIM_PROVIDER_KEYS_CONFIGURED_IN_UI=true
VERBATIM_PUBLIC_SITE_URL=https://example.com
