# Verbatim deployment environment reference
#
# Verbatim is static and does not read server-side env vars at runtime.
# Provider keys are entered in the app UI by each user.

VERBATIM_PROVIDER_KEYS_CONFIGURED_IN_UI=true
VERBATIM_PUBLIC_SITE_URL=https://example.com
